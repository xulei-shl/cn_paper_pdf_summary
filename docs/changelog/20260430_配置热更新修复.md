# 开发变更记录
- **日期**: 2026-04-30
- **对应设计文档**: 不涉及，本次为线上配置热更新缺陷修复

## 1. 变更摘要
- 修复 `.env` 删除或修改后，长驻进程继续沿用旧环境变量的问题。
- 修复 API 队列进程缓存 `config/config.yaml`，导致上传开关修改后不生效的问题。
- 补充回归测试，确保环境变量与工作流配置的热更新行为可验证。

## 2. 文件清单
- `utils/summary_uploader.py`: 重构 `.env` 同步逻辑，支持删除旧键并即时刷新进程环境变量
- `utils/api_queue.py`: 增加配置文件修改时间检测，按需重新加载 `config/config.yaml`
- `tests/test_summary_uploader.py`: 新增 `.env` 热更新与配置热更新测试

## 3. 测试结果
- [x] `python -m pytest tests/test_summary_uploader.py tests/test_notifier.py`
- [x] `python -m py_compile main.py api.py utils/api_queue.py utils/notifier.py utils/summary_uploader.py`
- [x] 核心路径验证通过
